# 聖書学習記録アプリ — 引き継ぎ資料

新しいチャットで作業を続けるための要点です。**このファイルと `BibleTracker.jsx`（または `bible-tracker.zip`）を一緒に渡してください。**

---

## 1. これは何か

デボーション（日々の聖書通読）と日曜のメッセージを記録するための、個人用アプリ。
React 単一ファイル（約3,450行）で作られている。

作者は**プログラミングの専門家ではない**。技術用語は避け、何をしたか・なぜそうしたかを平易な日本語で説明すること。

---

## 2. 成果物は2つ（中身は同一）

| ファイル | 用途 |
|---|---|
| `BibleTracker.jsx` | Claudeのアーティファクトとして開く版 |
| `bible-tracker.zip` | GitHub Pages で公開する独立版（Vite + React + Tailwind） |

**重要**: 保存処理を共通化したので、2つのファイルは**完全に同一**。
片方を直したら必ずもう片方にもコピーし、`diff -q` で一致を確認すること。

```
cp BibleTracker.jsx /mnt/user-data/outputs/BibleTracker.jsx
cp BibleTracker.jsx bible-tracker/src/App.jsx
```

zip の公開手順は `bible-tracker/README.md` に記載済み（GitHub Actions で自動デプロイ）。

---

## 3. 作業のルール（過去に事故があったため厳守）

1. **編集後は必ずビルド確認する。** esbuild が使える：
   ```
   ESBUILD=/home/claude/.npm-global/lib/node_modules/tsx/node_modules/.bin/esbuild
   cp BibleTracker.jsx /tmp/rtcheck2/App.jsx
   cd /tmp/rtcheck2 && $ESBUILD entry.jsx --bundle --format=iife --platform=browser \
     --alias:lucide-react=./lucide-mock.js \
     --alias:react=/home/claude/.npm-global/lib/node_modules/react \
     --define:process.env.NODE_ENV='"development"' \
     --loader:.js=jsx --outfile=bundle.js
   ```
   `/tmp/rtcheck2/` は毎回消えている。無ければ `entry.jsx` と `lucide-mock.js` を作り直す。
   `--alias:react=...` を付けないと react が見つからず失敗する。

2. **ビルドが成功したか必ず確かめてから試験する。**
   esbuild が失敗しても古い `.cjs` が残っていると、そのまま動いて「合格」に見えてしまう。
   試験の前に出力ファイルを消し、`tail -2` ではなく `head -30` でエラーを確認すること。
   （実際にこれで、古いビルドの結果を一度信じかけた）

3. **ビルドだけでは足りない。実際に描画させる。**
   `react-dom/server` の `renderToString` で各画面を描くと、構文では見つからない実行時エラーを拾える。
   `App.jsx` の末尾に一時的に `export { ... }` を足せば個別の画面を取り出せる（配布ファイルには残さないこと）。
   記録が空の状態も必ず試す。

4. **配布後に一致を確認する。** 過去に、置換の範囲を誤って `ContinueCard` などが丸ごと消えたまま配布し、アプリが起動しなくなった事故がある。
   `grep -o "^function [A-Za-z0-9_]*"` で編集前後の一覧を作り `comm` で差分を見ると、消えた部品に確実に気づける。

5. **python の一括置換は範囲に注意。** 閉じタグの重複・欠落が起きやすい。置換後は必ず前後を目視すること。

6. **ネットワークは使えない。** localhost も含めて遮断されている。npm install もできない。

7. **見た目は画像にして目で見る。** `sharp` と Chromium（`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`、playwright 経由）が使える。
   Tailwind は入れられないので、使っているクラス名を拾って簡易CSSを自前で作り、SSRしたHTMLに当ててスクリーンショットを撮る（前回 `/tmp/rtcheck2/minitw.cjs` として作成）。
   画像が表示できないときは、Chromium で要素の座標や背景色を測って数値で確かめる方法が有効。
   過去に確認せず納品して「かわいくない」と言われた。

---

## 4. 画面構成

下部タブは4つ：**ホーム / 記録 / 探す / 実績**
全画面の右上に三本線メニュー（`SideMenu`）。中身は「画面のカスタマイズ」と「バックアップ」。

### 記録の種類（4つ）
`TYPE_LABELS = { reading: "通読", message: "学び", memorization: "聖句", memo: "その他" }`（「疑問」は廃止）

過去に「お話」という種類を作ったが**廃止済み**。復活させないこと。
（記録画面の説明文に「お話」が残っていたのを削除した）

### 主要コンポーネント
- `RecordForm` — 記録の作成・編集（種類ごとに項目が変わる）
- `RecordDetailScreen` — 記録の閲覧。右上「編集」で編集へ
- `SearchScreen` — 探す。絞り込みは折りたたみ式
- `ProgressScreen` — 実績（カレンダー＋書ごとの通読、折りたたみ式）。イラストは末尾の1枚だけ
- `HomeScreen` — 聖句カード・続きから・バックアップ案内・果樹（いちばん下）
- `BookRecordsScreen` — 書をタップしたとき。上部に通読タイル、下に記録一覧
- `DayRecordsScreen` — 実績の「日ごとの記録」で日付をタップしたとき
- `ArtworkScreen` — 画面のカスタマイズ（テーマカラー・イラスト・ひとこと）
- `BackupScreen` — 保存と復元

---

## 5. 特徴的な仕組み

### 聖書箇所の自動認識
本文に含まれる「(申命記 6:5 SKY17)」のような引用や「ヨハネ 3章」を自動で読み取る（`parseBibleRefs`）。
- 「列王記 第二」のようにスペースが入る形にも対応済み
- 節がなく章だけでも認識する
- これが検索の絞り込み・関連記録・実績の集計すべての土台になっている

### ドラム式ピッカー
書・章・節はドラム（日付だけはカレンダー）（`WheelColumn` / `DrumSelect`）で選ぶ。
慣性つきの自前実装。ブラウザのスクロールに任せると上方向が効かなくなるため、`pointerdown/move/up` を `passive:false` で自前処理している。**安易にスクロール方式へ戻さないこと。**

### 左端スワイプで戻る
`useEdgeSwipeBack`。画面左端に透明な帯（`stripRef`）を敷いて検知する。
ボタンに判定を邪魔されないための構造なので、帯をやめないこと。

### イラスト
6種類の初期絵（ひつじ・ろば・ハト・さかな・らくだ・ぶどう）を内蔵。
ユーザーが自分の絵を最大5枚まで登録できる。
`MASCOT_GROUPS`（6分類）ごとに割り当て、未登録の分類には初期絵が出る。

**`MASCOT_SPOTS` は「どこに絵が出るか」の一覧で、カスタマイズ画面にそのまま表示される
（場所の名前と「○か所」という件数）。イラストを増やしたり減らしたり移動したら、必ずここも直すこと。**
実際の `<Mascot seed="...">` と突き合わせて確認する。`form-*` と `detail-*` は
`seed={"form-" + type}` のように動的に組み立てているので、seed の文字列を検索しても見つからない点に注意。
（実績の達成度の下にあった絵を消したとき、この一覧に消し忘れが残っていた）

### 未保存件数のバッジ
`unsavedCount()` が「前回の書き出し以降に作成・更新された記録数」を数える。
三本線・メニュー内・保存直後のトースト・ホームのカードの4か所で知らせる。
**三本線のバッジだけは数字を出さず、小さな丸（オレンジの点）にしている。**
数字入りだと三本線が隠れてしまったため。丸は上端から4〜18px、三本線の一番上の線は20pxなので重ならない。
位置やアイコンの大きさを変えるときは、この重なりを測り直すこと。件数はメニューを開けば分かる。

### 記録の作り方（＋を押したあとの流れ）
＋ → `TypePickSheet`（種類を選ぶシート）→ その種類の入力画面、という流れ。
種類を選んでから入るので、フォーム内の「種類」選び直し欄は出さない（`typeLocked`）。
間違えたときは閉じてもう一度＋を押す。
`openNewReading`（ホームの「続きから」）も種類固定で開く。

### 途中保存と自動下書き
記録の入力中に失われないよう、2段構えにしている。
- **途中保存**：ヘッダの「途中保存」ボタン。`onSave(rec, { keepOpen: true })`。
  同じidを上書きするので**何度でも繰り返し押せて、記録は増えない**。
  ボタンは**文字ではなくアイコン**（`SaveArrowIcon` 下向き矢印＋バー ／ `SaveCheckIcon` チェック）。
  押すと1.8秒だけチェックに入れ替わり、ボタンが濃いティールで塗られる。
  2つのアイコンを重ねて置き、透明度と拡大率で入れ替えているので幅は動かない。
  最後に保存した時刻はアイコンの**左**に出す（未保存の入力があるときは薄い色）。
  **このとき `setEditing` / `setIsNew` を呼ばないこと。**
  呼ぶと「新規」から「編集」へ画面構造が変わり、入力欄が作り直されて
  打っている最中のカーソルが外れる（実際にそうなった）
- **自動下書き**：入力が止まって0.8秒後と、アプリが背面に回ったとき（`visibilitychange` / `pagehide`）に
  `bible-tracker-draft` へ控える。次に開いたときホームに `DraftCard` が出て、続きから書ける。
  保存・削除・キャンセルで片付ける

### 種類の切り替えと厳格モード（重要）
`RecordForm` で「種類を変えたら中身を作り直す」処理は、
**`preparedForRef`（前回どの種類で用意したか）で判定している。**
「初回かどうか」を `useRef` のフラグで判定すると、Reactの厳格モードでは
効果が2回走るためすり抜け、開いた直後に中身が作り直されてしまう。
これが原因で「続きから」の引き継ぎと下書きの復元が動かなくなっていた。
**何回走らせても結果が同じになる書き方を保つこと。**

### 手ざわりの演出（`ft-*`）
グローバルCSSに `.ft-*` の演出をひとまとめにしてある。**新しく動きを足すときも、ボタンごとに `active:scale-…` を書かず、
`.ft-tap` などの共通クラスを使うこと。**
ばらばらに書くと、少しずつ深さや速さがずれて全体の手ざわりが揃わなくなる。

| クラス | どこで使うか |
|---|---|
| `.ft-tap` | 押すと沈む。全ボタン共通の土台（`BTN_BASE` にも入っている） |
| `.ft-tap-card` / `.ft-tap-icon` | 沈みの深さ。大きなカードは浅く、小さなアイコンは深く |
| `.ft-tap-pressed` | 押してから画面が変わるまで沈めておく（`useTapThen` と対で使う） |
| `.ft-rise` | 各タブの中身が立ち上がる |
| `.ft-tabswap` | タブの入れ物。**透明度だけ**（動かすと中の sticky ヘッダがぶれる） |
| `.ft-tabpop` / `.ft-tabbar` | 下タブのアイコンが弾む／下線が伸びる |
| `.ft-fab` | ＋ボタン・編集ボタンが回って出る |
| `.ft-ring` | 途中保存できたときの波紋 |
| `.ft-mark` | ピン留め・ブックマークを付けた瞬間 |
| `.ft-chev` / `.ft-chev-on` | ▽印のばね回転 |
| `.ft-open` / `.ft-open-y` | 折りたたみの中身。**中にドラム（fixed のシート）がある場所は必ず `.ft-open`（透明度だけ）** |
| `.ft-seq` | 子要素が順に現れる（シート・メニュー・検索結果） |
| `.ft-page-l` / `.ft-page-r` | カレンダーの紙送り |
| `.ft-daypop` | 選んだ日にちが前に出る |
| `.ft-grow` / `.ft-breathe` | 木が立ち上がる／収穫できるときだけ息をする |
| `.ft-chip` / `.ft-check-in` | 聖書箇所チップ・チェックの出現 |

**「その瞬間だけ」弾ませたいものは、`key` を切り替えて描き直させている**
（下タブのアイコン、ピン留め、選んだ日にち）。クラスを足すだけでは、
すでに置かれている要素は動かない。

10px前後・0.2〜0.4秒の控えめな動きに抑えること（大きく動かすと画面が揺れて煩わしい）。
- **`animation-fill-mode` は必ず `backwards` にする。** `both` だと終了後も `transform` が残り、
  中にある `position:fixed` の要素（ダイアログ）の位置の基準がその要素になってしまう
- タブは `<div key={tab} className="anim-tab">` で包み、切り替わるたびに動かしている
- **動く入れ物の中に `position:fixed` の要素を置かないこと。**
  動いている間だけ位置の基準がその入れ物になり、別の場所から飛んでくるように見える。
  記録タブの＋ボタンがこれで「上から落ちてくる」状態になったため、`AppMain` 側の
  動かない場所へ移した（`RecordScreen` の中に戻さないこと）
- 下から出るシートは `ft-sheet-up`。8px行き過ぎてから定位置に戻る小さなバウンド付き（0.36秒）
- 画面の切り替わりは `ft-zoom-in`（99%→100%）。ごくわずかに大きくなるだけに留めること
- 端末側で「視差効果を減らす」設定のときは動かない（`prefers-reduced-motion`）
- **カスタマイズ画面の「押したときの動きをつける」を切ると、`ft-still` が付いて全部止まる。**
  止めるのは動きだけで、読み込み中のくるくる（`.spin`）は残す指定にしてある
- ドラムを1項目回すたびに、ごく短く震わせている（`navigator.vibrate(4)`・40msに1回まで）。
  iPhoneのSafariでは何も起こらないが、害はない。演出を切っていると鳴らさない

### 上部の余白（切り欠き対策）
`SAFE_TOP(px)` を全画面のヘッダに当てている（`calc(env(safe-area-inset-top) + px)`）。
`index.html` が `viewport-fit=cover` なので、これが無いとiPhoneの切り欠きに文字が隠れる。
**ヘッダを増やしたら必ず `SAFE_TOP` を付けること。** 現在8か所。

### 日付の扱い
`todayStr()` は端末の地域の日付を返す。
以前は `toISOString()`（世界標準時）を使っていて、日本では朝9時より前だと前日になっていた。
**`toISOString().slice(0,10)` を日付として使い回さないこと。**

### 果樹を育てる（ホーム画面の下部）
100日を1サイクルとして10段階（`STAGES`）で育つ。ホームのいちばん下に木とみことばだけを出す。
- 昇格の判定は**かつ判定**。`cycleCounts` が返す「通読した日数」と「記録の件数」の両方が条件に届いて初めて進む
- **件数は種類を問わず、すべての記録を数える**。木がきらめく条件（今日なにか記録したか）と揃えてある。
  日数は**通読**した日で数える（同じ日に何件書いても1日）
- 全種類を数えるぶん、必要件数は引き上げてある（100日で153件）。
  数え方を変えるときは `STAGES` の `count` も一緒に見直すこと
- 昇格しても通知やポップアップは出さない。気づいたら変わっている、を大事にする方針
- Stage 10 で実をタップすると収穫。`garden.harvests` に残り、`cycle.harvested` が立って二重収穫を防ぐ。
  収穫後も「このままにする」を選べば実のなった木を出し続けられる
- 実の種類は5つ（`FRUITS`）。植え替えると `cycle.startedAt` が今日になるため日数・件数は0から数え直しになる。
  **記録そのものと収穫の履歴は消えない。** 数え直しになる旨は `ConfirmReplantDialog` で必ず確認を取る
- ホームでの並びは「聖句カード → 続きから → 果樹 → バックアップ案内」
- 今日の記録が1件でもあると（種類は問わない）、木に金色のキラキラが出る（`FruitTree` の `sparkle`）。
  `.ft-sparkle` でゆっくり瞬く。SVG の図形なので `transform-box: fill-box` が要る（無いと中心で拡大しない）
- ウェブアプリとしての名前は **Footprints**（`manifest.json` と `index.html`）。
  アイコンはりんごの芽（Stage 2）。`public/` の3つのPNGは
  芽の絵を自動で切り出して中央に置いて作った（中心のずれ0px・四辺の余白を揃えてある）
- イラストは `FruitTree`（stage 1〜10 × 5種類）。Stage 1〜5 は種類共通、6〜10 は品種ごとに描き分ける
- 保存先は `bible-tracker-garden`。書き出し形式は version 3 になったが、
  **version 2 以前（garden なし）のファイルも読める。壊さないこと**

### 66巻の折りたたみ（実績画面）
`BOOK_GROUPS` が BOOKS の並び順を8つのまとまりに区切っている（モーセ五書／歴史書／詩歌書／大預言書／小預言書／福音書と使徒の働き／パウロの手紙／その他の手紙と黙示録）。
`from` / `to` は BOOKS の添字なので、**BOOKS の並びを変えたら BOOK_GROUPS も直すこと。**
66巻・1189章がちょうど収まっているか、変更時は必ず数え直す。
最後に読んだ書が入っているまとまりだけ、最初から開いた状態にしている。

---

## 6. 保存の仕組み

`storageGet` / `storageSet` に集約。専用ストレージ→localStorage の二段構え。

| キー | 内容 |
|---|---|
| `bible-tracker-records` | 記録本体 |
| `bible-tracker-illustrations` | 登録したイラスト |
| `bible-tracker-tags` | 登録済みのタグ一覧（メニューの「タグの整理」で編集） |
| `bible-tracker-prefs` | テーマ色・イラスト表示・動きの演出・文字の大きさ・最終バックアップ日 |
| `bible-tracker-captions` | 記録画面のひとこと |
| `bible-tracker-garden` | 育てている木と収穫した実 |
| `bible-tracker-draft` | 書きかけの記録（自動下書き） |
| `bible-tracker-headerbg` | ヘッダの背景に敷く絵（1枚） |

バックアップの書き出し形式は `{ app, version:3, exportedAt, records, artworks, garden }`。
**旧形式（記録の配列だけ）も読み込めるようにしてある。壊さないこと。**

### 保存の順番
`saveData()` は上から順に試し、だめなら次へ進む。
1. 共有シート（iPhoneの「ファイルに保存」）
2. 保存先を選ぶダイアログ（PCのChrome/Edgeなど）
3. ダウンロード（埋め込み表示のときは飛ばす）
4. すべて失敗 → `SaveFallbackDialog` を出し、「コピーする」でクリップボードへ

`copyToClipboard()` は新しい方式（`navigator.clipboard`）がだめなら古い方式（`execCommand`）も試す。
コピーはボタンを押した直後に走るので、以前より成功しやすい。

### 壊れたデータへの備え（重要）
`loadRecords` / `loadArtworks` は、保存されている中身が壊れていても必ず配列を返す。
配列でなければ空として扱い、記録らしくないもの（null など）は取り除く。`migrateRecord` も null を返せる。
**以前はここが無防備で、保存された中身が配列でなかったり null が混ざったりすると
`r.map is not a function` で画面が真っ白になり、操作もバックアップもできなくなった。**
この防御を外さないこと。

### 最後の砦（ErrorBoundary）
`export default App` は `ErrorBoundary` で `AppMain` を包んでいるだけ。中身は `AppMain`。
どこかで表示に失敗しても真っ白にはならず、復旧画面が出る。
そこから記録をクリップボードへ取り出せる（React の状態ではなく保存領域から直接読む）。
テーマ色のCSSは `AppMain` の中にあるため、**復旧画面の見た目は inline style で直接指定している。**
CSSが一切効いていない状態でも読めるようにするため。クラス名に置き換えないこと。

### 既知の制約
Claudeの埋め込み表示では**ファイルのダウンロードができない**（画面ごとJSONに遷移して戻れなくなる）。
そのため埋め込み時は手順3を飛ばす。
ホーム画面に追加した独立版なら共有シートから正常に保存できる。

---

## 7. やり残していること

**なし。**

### 直近の大きな作り替え（自由タグ・ヘルプ・「？」）
- **自由タグ `tags: string[]` をすべての記録に追加。**
  決まった一覧は持たない。`allTagsOf(records)` で実際に使われた言葉を集めて候補にしている。
  **タグが増えても探す側を直す必要はない**（記録の文字をそのまま照らし合わせているだけ）。
  `recordAllText` にタグを含めているので、言葉の検索にも自然に引っかかる
- **「参考資料」（`links`）を廃止。** URLはメモ欄に直接貼る仕組みにした。
  閲覧画面では `HighlightedText` がURLを押せるリンクとして描き、
  YouTubeは `youtubeUrlsIn(recordAllText(r))` から拾って、その場で再生できるようにしている
- **「その他」から「書」「テーマ」を廃止。** 日付・メモ・タグだけの、いちばん自由な記録になった
- **探すの「記録の種類」プルダウンを廃止**し、タグの絞り込みに置き換えた
- **メニューに「ヘルプ」を追加**（`HelpScreen` / `HELP_SECTIONS`）
- **画面に出しっぱなしの説明文を廃止**し、`HelpTip`（押しているあいだだけ出る「？」）に置き換えた。
  `Field` に `help` を渡すと、項目名の横に「？」が付く。
  吹き出しは `position: fixed` で、押した瞬間に位置を測って画面の端に寄せている
  （入力欄は縦に流れる箱の中なので、その中に置くと端が切れる）
- **画面名の説明文（subtitle）を廃止**し、画面名を大きくした（タブ27px／メニューから開く画面20px）

> **移行について（重要）**
> 項目を消すだけだと利用者の記録が黙って消える。`migrateRecord` で必ず移すこと。
> ・参考資料のURL → メモ欄の末尾　・さらに古い `youtubeUrl` → 同じくメモ欄
> ・その他の「書」 → タグ　・「テーマ」 → 短ければタグ、長ければメモ欄の先頭

### 文字の大きさ（小・中・大）
`prefs.fontSize`（"s"／"m"／"l"、既定は "s" ＝これまでと同じ）。
やり方は、`text-[…]` のクラスごとに大きさを上書きする形（`.ft-font-m` / `.ft-font-l`）。
画面じゅうの `text-[…]` を書き換えるのは現実的でなく、1か所で切り替えるほうが取りこぼしが無い。
**文字の大きさを新しく使うときは、上書きの一覧にも足すこと。** 足し忘れるとそこだけ小さいまま。
見出しは本文より控えめな倍率にしてある（同じ倍率だと画面の幅に収まらない）。

### 入力欄の文字は16px以上（`.ft-input`）
**16pxより小さくしないこと。** iPhoneのSafariは、それより小さい入力欄に触れると
画面を勝手に拡大し、横にも動くようになって書きづらくなる。
文字の大きさの設定からも入力欄は外してある。

### 重なる画面を開いているあいだ、うしろを止める（`useLockBackground`）
iPhoneでキーボードが出るとき、手前が position:fixed でも、
うしろの画面のほうが動いて位置がずれる。開いている枚数を数えて、
最後の1枚が閉じたときだけ元に戻すこと。**戻し忘れると、以後どの画面も動かせなくなる。**

### 起動中の覆い（スプラッシュ）
`index.html` の `#splash`。アイコンとくるくるを出し、読み込みが終わると
アプリ側から `window.__ftHideSplash()` を呼んでふわっと外す。
- **CSSと外す処理は index.html の中に直接書くこと。**
  別ファイルにすると、それを読み込むまで白いままで、白い画面を隠す意味がなくなる。
  読み込めなかったときは覆いごと外せなくなるので、外す手だても中に置く
- 覆いが外れないままのときは、3.5秒後に直し方の案内が出る（真っ白にはならない）
- `html, body` の背景も覆いと同じ色にしておくこと。白いままだと一瞬白く光る
- 読み込み中のアプリ側の画面も同じ色にする（外れる瞬間に色が変わらないように）

### 本文のURLは `<a href>` にしないこと（重要）
リンクとして置くと、iPhoneが「このリンクを開きますか？」と確認を出し、
2回押さないと開けない。取り消しても空の小窓が残る。
ただの押しボタン（`<button>`）にして、`InlineLink` から直接小窓を開くこと。
枠の `sandbox` に **`allow-popups` を入れないこと**。
中のページが別窓を開こうとして、同じ確認が出るもとになる。
閉じるのは「✕」「背景をタップ」「つまみを下へ払う」の3つ。

### 本文のURLは、ふつうのリンクにするだけ
`<a href target="_blank" rel="noopener noreferrer">`。色は落ち着いた青、下線なし。
- **自前で確認を出さないこと。** iPhoneは必ず「このリンクを開きますか？」を出すので、
  こちらでも出すと二度手間になる（実際そうなって戻した）
- **自前の iframe で映さないこと。** YouTubeなど多くのサイトが
  「他所の画面の中に出すこと」を断っており、白いままになる（これも作って戻した）
- 開いたあとの見せ方（下からせり上がるアプリ内ブラウザ）は端末が受け持つ

### 種類ごとの絵は、その種類だけに使う
`TYPE_GUIDE` の絵＝通読 `BookOpen`／学び `Play`／聖句 `Star`／その他 `BookMarked`。
**聖句にまつわるものは、どこでも `Star` を使うこと。**
`BookMarked`（その他の絵）や `Sparkles`（収穫した実の絵）を混ぜない。
実際、ホームの聖句カードだけ「その他」の絵になっていた。

### たたんで見せるときは `clampText` を使わない
`clampText` は**改行の数**で切るので、1行に長々と書かれた文だと何も切られず、
開いても閉じても同じに見える（実際そうなっていた）。
見た目で切るなら `line-clamp-2`。ただし次の2つを守ること。
- **`whitespace-pre-line` と一緒に使わない**（iPhoneで高さだけ全文ぶん確保される）。
  たたんでいる間は改行を空白に置き換えて渡す
- **`block` と一緒に付けない。** `line-clamp` は `display` を変える指定なので、
  どちらが勝つかで切れたり切れなかったりする（実際そうなった）

### ホームの聖句は「今月／今年」の切り替えで1枚だけ
切り替えたときは、**押した側から中身が入ってくる**（`ft-swap-r` / `ft-swap-l`、0.26秒）。
下のタブの切り替えと同じ調子にそろえてある。動く量は12pxまで。
カードの中だけの入れ替えなので、大きく動かすと落ち着かない。
選んだ側の字は `ft-tabpop`（下のタブと同じ弾み方）。
入れ替えをやり直させるため `key={tab}` を付けること。外すと動かない。
同じ側をもう一度押したときは何もしないこと（いちいち動くとうるさい）。
2枚並べると縦に長くなり、ホームのほかのものが押しづらくなる。
- 切り替えは**両方あるときだけ**出す（片方しか無いなら選ぶ意味がない）
- 閉じているときの本文は `minHeight` で2行ぶんを確保すること。
  確保しないと、切り替えたときに高さが暴れる
- 切り替えは `min-h-[40px]` 以上を保つこと

### ひとことは何行でもよい（`DEFAULT_CAPTIONS`）
記録画面の下に、絵の下へ中央そろえで出す。`whitespace-pre-line` を外さないこと。
カスタマイズ画面の入力欄は `TextArea` ＋ `ft-h-field`（最小48px）で、
行が増えれば自然に伸びる。

### 画面の下端の逃げ場（`pb-*`）
下の帯（タブ）は高さ84pxほどあり、中身の上に重なって出る。
**余白がそれより狭いと、いちばん下の文が帯すれすれになって読みづらい。**
ホーム・記録・実績は `pb-28`（112px＝28pxの逃げ場）。
探すだけ `pb-20` のまま（「はじめの状態がスクロールなしで収まる」ほうを優先しているため）。
ホームを広げるときは `/tmp/rtcheck2/tree.cjs` も一緒に見ること。
広げすぎると、木と段階の聖句がある画面が縦に収まらなくなる（実際はみ出した）。
測る試験は `/tmp/rtcheck2/clear.cjs`。

### 浮いている丸ボタン（＋・鉛筆）の重なりと逃げ場
- ＋ボタンは **`z-40`**。下の帯（`z-30`）より小さいと帯の下に潜って欠けて見える（実際そうなっていた）
- 記録の閲覧は巻き物の下に **`pb-28`** の逃げ場をとる。
  無いと、いちばん下に置いたもの（たたみ部品など）が鉛筆ボタンに隠れて押せない
- 新しく浮くボタンを足したら、`/tmp/rtcheck2/fab.cjs` で
  「隠れていないか」「下のものを隠していないか」を測ること

### メニューから画面へ移るとき、いま出ている画面を全部閉じること
`goFromMenu` で閉じ忘れると、その画面が下に残ったままになる
（記録の閲覧・書ごと・日ごとが抜けていた）。**画面を増やしたらここにも足すこと。**
試験は `/tmp/rtcheck2/menu5.cjs`。

### 三本線の大きさは1か所で決める（`MENU_BTN` / `MENU_ICON`）
タブの見出し（`ScreenHeader`）と重なる画面の見出し（`MenuButton`）で
別々の数にしていたため、画面を移ると大きさが変わって見えた。
測る試験は `/tmp/rtcheck2/header.cjs`。

### 共有シートに title を渡さないこと
iPhoneはこれを「共有する文章」と見なし、その文字だけを書いた
余分なテキストファイルまで作ってしまう（実際そうなっていた）。

### 入れ替えた直後は、ひと呼吸だけ動きを止めること（`settling`）
並びが変わると札は新しい位置へ移るのに、よけるために付けていた
ずれ（translateY）も同時に0へ戻ろうとする。どちらも動くと、
札が1行ぶん跳ね上がって見える（実測で25px跳ねていた）。
入れ替えの直後だけ transition を切り、静かに入れ替わったように見せる。
試験は `/tmp/rtcheck2/tagsettle.cjs`（離したあとを1コマずつ追って測る）。

### タグを動かすときは、跳ねる動き方を使わないこと
`cubic-bezier(.22,1,.36,1)` は行き過ぎてから戻る動き方なので、
よける札が跳ねて見える。`ease-out` にすること。
つまんだ札を大きくするのもやめた（動かすたびに膨らんで落ち着かない）。

### 並び替えのあとに「順に現れる動き」を掛け直さないこと
`ft-seq` は画面を開いたときの一度だけにする。
並びが変わると札の順番も入れ替わり、動きが必ず掛け直されて一斉に出直す。
指を離した瞬間に札が薄くなり、点滅して見える（実測で不透明度が0まで落ちていた）。
`seq` を持ち、つまんだ時点で false にすること。試験は `/tmp/rtcheck2/tagflash.cjs`。

### ラベルを左に置いて縦を詰めるとき
「記録の種類」の名前・説明のように、ラベルを入力欄の左に並べて縦を減らすことがある。
**入力欄そのものは小さくしないこと**（文字は16px以上、高さもそのまま）。
削るのはラベルの幅だけ（`w-12`）。
説明のように伸びる欄は `items-start` ＋ ラベルに `pt-3.5` で、1行目の高さに合わせる。
測る試験は `/tmp/rtcheck2/typerow.cjs`。

### イラストは「場所の数」までしか登録させない
場所より多く登録しても、あまった絵は出番がない。
そろったら「ここに絵を追加」を押せなくすること（`mine.length >= spots.length`）。
どの絵がどこに出るかは `PAIR_COLORS` で色を合わせて示す。
色は**その場所に実際に出る絵**に合わせること（`pairColor(i % mine.length)`）。
場所の順番だけで色を決めると、絵が1枚しか無いのに2つ目だけ違う色になり、
対応が読み取れなくなる（実際そうなっていた）。

### 年の選択肢
`jumpYears` が今年をまんなかに前後100年ぶんを返す。
それより外の年の記録を持っている人のために、その年までは足すこと。
足さないと、その記録の年月へ飛べなくなる。

### 「何を記録しますか」に行を足すとき
**絵と字の位置を `TypeRow` とそろえること。** 枠 `w-11 h-11`、絵の大きさ 22、
すきま `gap-3`、余白 `px-3 py-3`、題は `text-[15.5px] font-bold text-neutral-900`、
説明は `text-[12.5px] text-neutral-500 mt-0.5`。
数がひとつ違うだけで、並びがずれて見える（実際ずれていた）。
測る試験は `/tmp/rtcheck2/rowalign.cjs`。

### 記録を1件だけ受け渡す（`oneRecordJson` / `recordsFromFile`）
Footprintsを使う人どうしで記録を分け合うため。全件のバックアップとは目印を分けてある
（`app: "footprints-record"`）。
- 書き出すとき **id・ピン留め・ブックマークは持ち出さない**（その人の目印なので）
- 取り込むとき **いまある記録を消さないこと。** 足すだけにする。
  id は必ず振り直す（送り手の id が偶然かぶると上書きしてしまう）
- 中身がそっくり同じものは足さない（同じファイルを二度取り込んだとき用）
- 受け取ったタグはタグ一覧にも控える
- **数え上げを `setRecords` の中でやらないこと。** あの中は2回呼ばれることがあり、
  数がずれる（実際ずれた）。足すものを先に決めてから渡す

### 知らせ（トースト）を出すときは、前のタイマーを止める
止めないと、続けて操作したときに前のタイマーが新しい知らせを消してしまう（実際そうなった）。
`tellImport` / `tellShare` のように、消しタイマーを1本に保つこと。

### バックアップは JSON と TXT の2つを出す
TXTの中身は **「内容を確認する → 読みやすい形式」と同じもの**（`buildBackupText`）。
別に組み立て直すと、画面で見えているものと食い違う。

### 聖句は引用ブロックで見せる（`splitByQuote`）
閲覧画面では、引用（本文＋聖書箇所）を**左にテーマ色の縦線を引いた段落**として描く。
文字は本文よりすこし落ち着いた色（`text-neutral-600`）。大きさと行間は本文と同じ。
**斜体にはしないこと**（日本語だと読みづらい）。
縦線は `.ft-quote` で引く。**太さ・線種・色をこの1か所で決めること。**
Tailwind の `border-l-[…]` は「太さ」しか決めず、線種は土台の指定に頼るため、
環境によっては線種が none のままで線が出ない（実際そうなっていた）。
色はテーマ色を白と混ぜた淡いトーン（`color-mix`）。透かし（`/30` など）は使わない。
背景の色によって濃さが変わってしまうため。
**右端は入れ物の右端のままにすること。** 縦線と字下げは左だけに付けるので、
ふつうの文と右端がぴったり揃う（実測で確認する試験が `/tmp/rtcheck2/quote.cjs`）。
引用の中では聖書箇所への色付けをしない（全体で整えているため）。

### 色を付ける範囲は `splitByCitations` にまかせる
「聖句に追加」の対象と、閲覧画面で色が付く範囲は**必ず同じ関数から取ること**。
別々に書くと食い違う（実際そうなっていた）。
引用と見なすのは、①括弧に入れた箇所（全角・半角とも）と
②行の終わりに置かれた箇所の2つ。文の途中の箇所は引用にしない。
※`REF_REGEX` を回している最中に `parseBibleRefs` を呼ばないこと。
読み取り位置が先頭に戻され、終わらなくなる。位置を先に集めてから調べる。

### ホーム画面の木の大きさ（`TREE_SIZE`）
**植える前と植えたあとで必ず同じ数を使うこと。** 別々にしていたため、
植える前だけ小さく見えていた（168px と 190px）。いまは 220px でそろえてある。
ホーム画面のいちばん大事な絵なので、これ以上小さくしないこと。
大きくするときは `/tmp/rtcheck2/tree.cjs` で、画面に収まるかを測ってから決める。
※段階ごとの聖句の長さで縦が伸び縮みするので、小さい端末で少しスクロールが要るのは元からの仕様。

### アプリのアイコン（`public/icon-*.png`）
背景はごく淡い空色。土は絵の下端まで塗りつぶし、下に白い帯を残さない。
絵はアプリの3段階目（かわいい双葉）と同じ座標・同じ色をそのまま使っている。
寄り引きは `make-icon.cjs` の `GROUND_AT` / `LEAF_AT` で決まる（差が大きいほど双葉が大きく写る）。
変えたら、双葉が左右の端に当たっていないか測り直すこと（いまは左右とも27%ほど空いている）。
**アイコンの名前には版数（`-v2`）を付けてある。**
同じ名前のまま絵だけ差し替えても、端末とGitHubに残った古い画像が出続けて変わらない。
描き替えたら `make-icon.cjs` の `V` を1つ増やし、`index.html` と `manifest-v?.json` も
その名前に合わせること（設定ファイル自体も名前を変える。中身だけ替えても
古い設定が残っていると古いアイコンを指し続ける）。
なお**iPhoneのホーム画面に置いたアイコンは、置いた時点の絵が焼き付く。**
名前を変えても、いったん削除して置き直さないと変わらない。

作り直すときの注意：
- **`sharp` の `density` を上げたまま書き出さないこと。** その分だけ大きく描かれ、
  `manifest.json` に書いた大きさと食い違う（実際 512 が 2731 になっていた）。
  必ず `resize` で所定の大きさへ戻し、書き出したあと大きさを確かめる
- **絵の広がりを目分量で決めないこと。** いちど描いて測ってから拡大率を出す。
  目分量だと土が下の端に当たったり葉が切れたりする（実際そうなった）
- `apple-touch-icon.png` は四隅を透明にしない。iPhoneで黒く出ることがある

### 重なる画面に `key` を付けないこと（重要）
`{viewing && <RecordDetailScreen …/>}` のように並んでいる覆いのうち、
**ひとつだけに `key` を付けると、画面が二重に残る。**
実際に `key={viewing.id}` を付けたところ、
「実績 → 日ごとの記録 → 記録を開く → 編集 → 保存」で閲覧画面が2枚になり、
戻るたびに同じ画面が出てくる状態になった。
出てくる向きの動きは `from` の切り替えでやり直されるので、`key` は要らない。
試験は `/tmp/rtcheck2/repro.cjs`（この手順をそのままなぞる）。

### 記録の閲覧画面が出てくる向き
`RecordDetailScreen` の `from`。ふつうは右から（`"right"`）。
ちょっと見る小窓の「この記録を開く」からだけ、下から（`"bottom"`）。
小窓が下から出ているので、そのまま続けて上がるほうが自然なため。
`openDetail(record, "bottom")` のように渡す。
**`key={viewing.id}` を外さないこと。** 外すと、別の記録に移っても
出てくる動きがやり直されない。

### 記録画面の「最近の記録」
並べ替えは `updatedAt || createdAt` の新しい順で10件。
**作った日ではなく「最後に手を入れた日」で並べること。**
作った日で並べると、古い記録を書き直しても下に埋もれたままになる。

### タグの整理（`TagManageScreen`）
名前を変える・消すときは、**一覧（`tagMaster`）と記録の両方に同じことをすること。**
片方だけ直すと、記録に古い名前が残って食い違う。`renameTag` / `deleteTag` が受け持つ。
タグを消しても記録そのものは消さない（タグを外すだけ）。

### 今月・今年の聖句の付け替え
すでに別の記録に付いているときは `HighlightTakeoverDialog` で確認する。
**元の記録から外すのは「保存」のときだけ。** チェックした時点で外すと、
そのあとキャンセルされたときに元の記録だけ印が消える。
入力画面は `steal`（どれから外すか）を覚えておき、`onSave(rec, { steal })` で渡す。

### 下からせり上がる小窓（`ft-sheet-wrap` / `ft-sheet-box` / `ft-sheet-body`）
タグを選ぶ・記録をちょっと見る・ドラム・種類選びの4つで共通に使う。
**この3つのクラスを必ずセットで使うこと。** 自前で `fixed inset-0` と書かないこと。
- 高さは `dvh`（いま見えている高さ）。`vh` だけだと iPhone でブラウザの帯のぶん下に伸び、
  いちばん下のボタンが画面の外へ出る
- `margin: 0` が要る。`space-y-*` の中に置くと外側の余白が足され、その分だけ下へずれる
  （探すの絞り込みで、実際に12pxずれてボタンが隠れていた）

> 試験は `/tmp/rtcheck2/sheet.cjs`。**寸法用のCSSに `items-end` を入れ忘れると、
> 小窓が引き伸ばされて不具合を見逃す。** 実際いちど見逃しかけた。

### 重なり順（z-index）は、要素に直接書く
`HelpTip` の吹き出しのように「まわりの箱より前に出したい」ものは、
クラスではなく `style={{ zIndex: … }}` で与えること。
クラス任せにすると効かない場面があり、あとに置かれた箱の下に潜り込む。
実際、聖句の入力欄で「今年の聖句」のカードに隠れていた。
（`/tmp/rtcheck2/layer.cjs` に、隠れる条件を切り分けた試験がある）

### 色の数値を画面の部品に直接書かない
ドラムの選ばれた行の色が緑のままだった。テーマを変えても追随しない。
`var(--th-800)` のようにテーマの色を使うこと。
※イラスト（`FruitTree` や `Mascot`）の中の色は、絵柄そのものなので固定でよい。

### テーマ「空」の key は `indigo` のまま
もとは藍色だったが、菫と見分けがつかず空色に差し替えた。
**key を変えないこと。** 変えると、その色を選んでいた人の設定が既定に戻る。

### 何行かだけ見せたいときは `clampText`
**`line-clamp-*` と `whitespace-pre-line` を一緒に使わないこと。**
iPhoneのSafariでは見た目だけ切り詰められ、箱の高さは全文ぶん確保される。
たたみ部品の中に大きな余白ができる原因になっていた。
`clampText(text, 行数)` で文字そのものを切ってから渡すこと。

### 記録の種類「疑問」と「疑問メモ」は廃止した
タグで表せるようになったため。**復活させないこと。**
古いデータは `migrateRecord` で移している：
- 種類「疑問」の記録 → 「その他」に移し、本文はメモ欄へ。タグ「疑問」を付ける
- ほかの記録に付いていた疑問メモ → メモ欄の末尾へ。タグ「疑問」を付ける
- どちらも、解決済みだったものにはタグ「解決済み」を付ける

### 画面に出す名前は、書き決めにしない
記録の種類の名前はカスタマイズ画面で変えられる。
**「通読」などを文字で直接書かないこと。** そこだけ古い名前が残る。
イラストの見出しと場所の名前は `mascotGroupLabel` / `mascotSpotLabel` が
`nameDraft`（設定中の名前）から組み立てている。新しい場所を足すときも同じようにすること。
ボタンの名前を文中で引くとき（「『保存』を押すと…」）も、実際のボタンと必ず突き合わせること。

### 「？」の消し方
時間では消さない。消すのは**周りを触ったとき**と、もう一度「？」を押したときだけ。
読み終わる速さは人それぞれなので、勝手に引っこめない。

### カレンダーの見出しは共通部品（`MonthNavHeader` / `MonthJumpSheet`）
実績画面のカレンダーと、日付を選ぶ欄の見出しは**同じ部品**を使う。
`＜ 年月 今日 ＞` の並びで、年月を押すと年月のドラム（`MonthJumpSheet`）が下から出る。
**片方だけ書き換えないこと。** 押しやすさや動きが食い違う。
小窓の上にさらに小窓を重ねるときは、`WheelSheet` の `zIndex` に大きい数を渡すこと
（日付の小窓が 2147483000、年月の小窓が 2147483100）。

### 日付はカレンダーから選ぶ（`DateInput`）
ドラム式だと曜日が分からず「いつの話か」が思い浮かびにくいので、カレンダー式にした。
**日付を登録するところは、すべて `DateInput` を使うこと**（記録の日付・探すの期間）。
曜日はカレンダーの見出し（日〜土）だけに出す。**日付のとなりには添えないこと**（依頼により削除）。
曜日の並びと色は `WEEK_LABELS` / `weekColor` に一本化してある（実績のカレンダーと共通）。
小窓の作りは `ft-sheet-wrap` / `ft-sheet-box` / `ft-sheet-body` の決まりに従うこと。

### バックアップのファイル名
`Footprints-backup-2026-07-28.json` の形。
**半角の英数字とハイフンだけにすること。** 空白や日本語を混ぜると、
共有や送信の途中で文字が化けることがある。
日付は後ろに置く（並べたときに古い順に揃うため）。

### 復元したら「最後に書き出した日」を今にすること
読み込んだ記録は、もう書き出し済みのもの。そのままだと
「まだ書き出していない記録が…」と促してしまい、同じ内容をもう一度書き出すことになる。
`handleRestore` で `lastBackup` を今の時刻にする（設定が入っていないファイルのときも忘れずに）。
※1件だけの取り込み（`importOneFile`）は新しい記録なので、こちらは触らない。

### バックアップに入るもの（version 6）
`records` / `artworks` / `garden` / `tags` に加えて、**画面の設定も入る**。
`headerBg`（ヘッダの背景・version 6 から）。
`prefs`（テーマ色・イラスト表示・動きの演出・文字の大きさ）、
`captions`（ひとこと）、`typeDesc`（記録の種類の名前と説明）。
機種を変えたときに、整えた見た目まで戻ってくるようにするため。
- **`prefs.lastBackup` は入れないこと。** 「この端末でいつ書き出したか」であって、持ち運ぶものではない
- 設定が入っていない古いファイル（version 4以前）を読み込んだときは、**いまの設定をそのまま残す**
- 新しく設定を増やしたら、ここにも足すこと。足し忘れると機種変更で消える

### タグマスタ（登録済みのタグ一覧）
保存キーは `bible-tracker-tags`（文字の配列）。バックアップにも `tags` として入る（version 4）。
**記録から集めるだけにしないこと。** それだけだと、記録を消したとたんタグも選べなくなる。
画面に出す一覧は `knownTags = normalizeTags([...tagMaster, ...allTagsOf(records)])`。
一覧と記録の両方から作っているので、前の版から引き継いだタグも最初から選べる。
新しく作られたタグは `addTagToMaster` で控える（決定を押さなくても残る）。

### 「探す」画面は、はじめの状態が1画面に収まること
絞り込みは開いた状態で始まる。**iPhoneでスクロールなしに収まる高さを保つこと。**
（実測：中身645px。iPhone 15/16相当660px・17相当740pxで収まる）
詰めるためにやってあること：
- `BookSelect` に `compact`（旧約・新約の切り替えを選ぶ欄の左に並べ、1行に収める）
- 画面の下余白は `pb-20`（＋ボタンが無い画面なので `pb-28` は不要）
- 絞り込みの中は `space-y-2.5` / `p-2.5`
行を増やすときは `/tmp/rtcheck2/fit.cjs` で、実際にスクロールが要るかを測ってから決めること。

### 絞り込みの部品は `FilterPill` にそろえる
記録の種類・疑問メモなど、押して切り替えるものはすべて `FilterPill`。
チェックボックスと混ぜないこと。

### 検索ボタンが押せる条件
`hasCriteria && dirty`。**`hasCriteria` に絞り込みを足し忘れないこと。**
疑問メモを入れ忘れていたため、疑問メモだけ選んでも押せない状態になっていた。
結果の出し分けは `applied.*`（押したときの状態）を使うこと。今の入力値を直接見ない。

### 一覧はダイアログの中に置く
タグの選択（`TagPickDialog`）は入力画面と検索画面で共通。
**タグの一覧を画面に出しっぱなしにしないこと。** 増えるほど画面を圧迫する。
検索の「記録の種類」だけは数が5つで増えないので、カプセル型を常時出している
（複数選択・トグル式。種類は「どれか」、タグは「すべて含む」で絞り込む。目的が違うのであえて揃えていない）。

### 「？」の出し方
タップで開き、4.5秒で自分から消える。ほかの場所を触ったときも消える。
**開いたそのひと押しで閉じてしまわないよう、外側の見張りは `setTimeout(…, 0)` の後から始めること。**

### 丸い印は、指定（style）で大きさを与える
「？」（`HelpTip`）と件数のバッジ（`CountBadge`）は、
Tailwindのクラスではなく `style={{ width, height, borderRadius: 9999 }}` で作っている。
**縦横を同じ数で固定すること。**
以前は件数バッジを `min-w-[22px] + px-1.5` で作っていたため、
桁が増えると横長の楕円になっていた。いまは桁が増えたら文字のほうを小さくして丸を保つ。

### 果樹の言い回し（重要）
最後の段階は **「100日**かつ**153件」の両方**が要る。日数だけ経っても実らない。
そのため **画面に出す文には「100日」と書かないこと。**
日数さえ過ぎれば実るかのように読めて、実際と食い違う。
「記録を重ねて実を育てる」「続けるほど実る」のように、両方が要ることが伝わる言い方にする。

### 押してから画面が動きはじめるまでの間
**「動きはじめる時刻」と「動きの速さ」は別物。混同しないこと。**
- 動きはじめる時刻 … `useTapThen` の既定値（現在 **60ms**）。実測で約78ms
- 動きの速さ … `.anim-right` の0.26秒など。**ここは変えていない**

**動きはじめる時刻を縮めるときは、必ず `.ft-tap-pressed` の速さも一緒に見ること。**
暗くなる途中で画面が切り替わると、押した手ごたえが見えないまま消える。
いまは45msで暗くなりきり、その直後に動きはじめる。

### 字の太さは600まで

`.font-display` は `Noto Sans JP` の600。Tailwind の `font-bold`（700）も
600へゆるめてある。姉妹アプリ（My手帳）と同じ。
**900や700に戻さないこと。** 重い字が並ぶと、それだけで画面が固く見える。

`@import` は `<style>` の**いちばん先頭に置くこと**。
前に別の指定があると、ブラウザはこの行を読み飛ばし、書体が当たらない
（一度そうなっていた）。

### 記録を守るための約束

- **起動時に `askPersist()` を呼ぶこと。** `navigator.storage.persist()` に
  「消さないでほしい」と頼む。断られても害はないので、返事は待たない
- **まだ控えていない書きかえは、一度だけ知らせること。**
  いまは三本線の印（`MenuIconWithBadge`）と、メニューの行、
  バックアップ画面の上の帯の3か所。これ以上増やすと、ただの飾りになって
  読み飛ばされる
- 書き出したら `markBackedUp()`（`prefs.lastBackup`）を必ず通すこと。
  通し忘れると、控えが古いままなのに「取ってある」と見えてしまう
- 置き場の余裕（`storageRoom()`）は**数を決め打ちしないこと。**
  端末と空き容量で大きく変わる。取れない端末では何も出さない

### 「この箇所を含む記録」も節まで見る（`relatedRecords`）

閲覧画面の札も `recordScopeRefs` ＋ `refsOverlap` で選ぶ。
**`recordRefs` と「書と章が同じか」で判定しないこと。**
そうしていたため、3:13-24 を開いているのに 3:1-12 が並んでいた。
入力画面の「過去のメモ」と同じ道具を使うこと。試験は `/tmp/rtcheck2/rel.cjs`。

### 起動の画面（スプラッシュ）と版数

- **版数は `index.html` の `window.__FT_VERSION` 1か所で決めること。**
  起動の画面の小さな字も、使い方画面の下の字も、同じ値を出している
  （`App.jsx` の `APP_VERSION` は、index.html が無いアーティファクト版のための控え。
  **両方そろえること**）
- **手を入れたら、その都度かならず版数を上げること。**
  上げかた（X＝見た目や操作性の大きな変更・ページ追加／Y＝細かな機能向上・部分的な修正／
  Z＝不具合の直し・誤字脱字）と、これまでの変更は `CHANGELOG.md` に書いてある。
  **Zは「直し」だけ。** 動きや見た目を新しく変えたのなら、小さくてもY以上を上げること。
  上げ忘れると、直したはずの版と手元の版が同じ数になり、
  「直っていない」のか「古い版のまま」なのかを切り分けられなくなる
- **かならず最低2秒は見せること（`FT_SPLASH_MS`）。**
  読み込みが速い端末だと、一瞬だけ光って消え、かえって目ざわりになる
- くるくる（読み込み中の輪）は出さない。アプリ名と双葉が主役
- **この覆いのCSSと外す手だては、`index.html` の中に直接書くこと。**
  組み立て後のファイルに置くと、それを読み込めなかったときに
  外す手だてごと失われる

### 貼りつく部品は `--ft-head-h` の下に置く

`ScreenHeader` が自分の高さを測って `--ft-head-h` に書いている。
「探す」の検索欄と絞り込みの帯は、その下に貼りついている。
**数を書き写さないこと。** 文字の大きさを変えると見出しも高くなり、食い込む。
絞り込みの中身は貼りつけない（開くと背が高く、結果を見せる場所が無くなる）。

### 記録を書く画面には項目名を出さない

何を書く欄かは、欄の中の薄い字（`placeholder`）で分かるようにしてある。
**名前と説明を並べないこと。** 書きたいことより先に字を読むことになって手が止まる
（姉妹アプリ My手帳 と同じ方針）。
日付・タグ・種類も、部品を見れば何を入れるか分かるので名前を出していない。
項目のあいだの余白は `mb-4`（16px）にそろえること。
「聖書箇所を挿入」は欄の 4px 下。**欄と印のふたつで、ひとつの項目**なので、
間を空けたり、認識した箇所の並びをはさんだりしないこと。

### 見た目の決めごと（姉妹アプリ My手帳 に合わせてある）

- **枠の線は1px。** 2pxにすると、欄が並んだときに線ばかりが目に入る。
  例外は「選んでいるかどうか」を色で見せる札やボタンだけ（そこは2pxのまま）
- 欄と欄のあいだは `mb-4`（16px）。広げると1画面に入る欄が減って書きにくい
- **「書・章・節を選んで挿入」は、対象の欄のすぐ下に置くこと。**
  認識した箇所の並びをはさむと、どの欄に入るのか分からなくなる

### 並べ替え（`useReorder`）

**指の位置は、いま並んでいる札の実際の位置（`getBoundingClientRect`）とくらべること。**
「1枚ぶんの高さ」を見積もって割り算する作りも書いたが、札の高さがそろわないときや
画面を送ったときに1つずれた。よけるためのずれ（`translateY`）と、
指を離したあとの跳ねを抑える細工（`settling`）も要らなくなった。

- 動かしている最中にその場で入れ替える。どこへ入るのかが目で分かる
- `touchAction: "none"` は**取っ手にだけ付けること。**
  札ぜんたいに付けると、指で画面を送れなくなる
- 取っ手の絵は `GripVertical`。**別の絵にしないこと**

### 説明文の書きかた

**要点だけ書くこと。**

- ？の中（`HelpTip` / `Field` の help）は1〜2文で収める
- 「URLをそのまま貼れます」「絵の上には自動でうすい白がかかります」のような、
  限られた場面の話や、こちら側の事情でそうしている話は書かない
- メニューの下に出る説明（`desc`）は、見出しで分かることを繰り返さない。
  「タグの整理」の下に「タグの追加…」と書かない、など

### 画面の高さと下の帯（重要・何度も間違えた）

**画面ぜんたいを「高さの決まった箱」にしないこと。**
`100dvh` で高さを決める作りも、`position:fixed; inset:0` で留める作りも、
JSで実測して当てる作りも試した。どれも端末によって下のはしが画面と食い違い、
**見出しが時計に重なったり、下に別の色の帯が出たり**した。
姉妹アプリ（My手帳）も同じ道をたどって、この形に戻している。

```
.ft-shell（min-height:100vh）   ← ふつうに縦へ伸びる。画面ごと送る
 └ 各画面（ScreenHeader は sticky top-0）
BottomNav（fixed bottom-0・ft-tabbar-wrap）
```

- 下のタブは `position: fixed; bottom: 0` に置く。**流れの中へ戻さないこと**
- `html, body` の `overflow` を止めないこと。重なる画面を開いているあいだだけ
  `useLockBackground` が一時的に止める
- **`html, body` の背景は白（`#FFFFFF`）にすること。**
  ホーム画面に追加したアプリでは、いちばん下の細い帯（ホームバーのところ）を
  iPhone が body の色で塗る。起動中の覆いに合わせた水色のままだと、
  そこだけ水色の帯が出たままになる。`index.html` 側の水色は消さないこと
  （消すと、読み込み中に白い画面が出る）

### 下の帯の厚みは `--ft-nav-h` ひとつで決める

```
:root { --ft-nav-h: calc(57px + env(safe-area-inset-bottom)); }  /* 中身56 ＋ 上の線1 */
```

- **タブの高さは `min-height` ではなく `height` で留めること。**
  `min-height` だと中身しだいで数pxふくらみ、`--ft-nav-h` と食い違う。
  食い違うと、三本線メニューの下の段だけ高さがずれて見える
- **下の帯だけは、文字の大きさの設定でふくらませないこと。**
  `.ft-tabbar-wrap .ft-tabbtn span { font-size: 11.5px !important; }`。
  ここが厚くなると、記録を見せる場所が減るうえ、右下のボタンが帯に重なる
  （実際そうなっていた）
- 右下の丸ボタン：`bottom: calc(env(safe-area-inset-bottom) + 96px)`、
  大きさは56px角（`w-14 h-14`）。＋（記録画面）と鉛筆（閲覧画面）で
  別々の大きさにしないこと
- 逃げ場は `.ft-pad-nav`（帯だけ）と `.ft-pad-fab`（帯＋丸ボタン）の2つ。
  **画面ごとに `pb-28` のような数字を書かないこと**
- メニューの下の段：`minHeight: var(--ft-nav-h, 56px)`
- 測る試験は `/tmp/rtcheck2/m2.cjs`（帯の厚み・ボタンとのすきま・
  メニュー下段との一致・文字「大」でふくらまないか・送っても動かないか）

### テーマ色を置く場所

**画面の下地（`.ft-page`）はテーマ色にしないこと。**
いちばん淡い調子（`var(--th-50)`）を敷いてみたが、画面ぜんたいが色づいて
主張が強すぎた。無地（`#FAFAFA`）に戻してある。

テーマ色は小さな場所だけに置く。いま置いているのは、
小見出し（`text-th-800/70`）・画面名の下の細い線（`border-th-200`）・
文字を選んだときの色・入力中の縦棒、それに元からあるボタンや札。
新しく小見出しを足すときも、灰色ではなく `text-th-800/70` に合わせること。

### 文字の大きさのはしご

大きさは `text-[◯px]` の値そのもので決まり、「中」「大」の設定は
`.ft-font-m` / `.ft-font-l` のはしごで拡大する。
**新しい大きさを使うときは、はしごに2段（中・大）を必ず足すこと。**
足し忘れると、その文字だけ設定を変えても大きくならない。

**素の値（＝「小」のとき）は 27 / 20 / 17 / 12.5 にすること。**
一度これを1段ずつ上げてみたが、「小」が大きくなりすぎた。
いまの見え方は次のとおりで、狙いは**「中」でちょうどよく見えること**。

| 用途 | 小 | 中 | 大 |
|---|---|---|---|
| 画面名（ホーム等） | 27 | 29 | 31.5 |
| 重なる画面の題 | 20 | 22 | 24 |
| ダイアログ・行の題 | 17 | 18.5 | 20 |
| 小見出し | 12.5 | 14 | 16 |
| 本文 | 14.5 | 16.5 | 19 |

大きくしたいときは、素の値ではなく**はしごの側を動かすこと**。
素の値を動かすと「小」だけが道連れになる。

### ヘッダの背景（`--ft-hdrbg` / `.ft-hdr`）

保存キーは `bible-tracker-headerbg`。ルートに `.ft-hasbg` が付き、
`--ft-hdrbg` に画像が入る。ヘッダの div には `.ft-hdr` を付けること
（`SAFE_TOP` を付ける場所と同じ）。

**絵の上には黒い膜をかけ、文字は白にすること。**
白い膜＋黒文字も作ってみたが、明るい写真で文字が沈んだ。
黒い膜＋白文字なら、どんな写真でも読める（姉妹アプリ My手帳 と同じ考えかた）。

```
linear-gradient(180deg, rgba(0,0,0,.34), rgba(0,0,0,.56))
```

- 白くするのは `[class*="text-neutral-"]` と `[class*="text-th-"]`。
  **自前の下地を持つボタンには `ft-onbg-keep` を付けて、ここから外すこと。**
  外すのを忘れると、明るい下地の上に白い字が乗って読めなくなる
  （いまは記録画面の「途中保存」ボタンだけ）
- カスタマイズ画面の見本にも、同じ膜と同じ文字色を当てること。
  片方だけ直すと、見本と実物が食い違う

### 聖書箇所の取り違え（`bookNameOkAt`）

「創」「出」「民」「申」「詩」などの**1〜2文字の略称**は、ふつうの日本語にも現れる。
そのままだと「国民 3割」が『民数記 3章』、「出3人」が『出エジプト記 3章』になり、
聖書箇所をひとつも含まない記録まで「同じ箇所」と見なされていた。

- 2文字までの略称のときだけ、①直前が漢字・英数字でないこと
  ②1文字の略称は「章」か「:」「：」が付いていること、を確かめる
- **この判定は `parseBibleRefs` と `splitByCitations` の両方で必ず通すこと。**
  片方だけにすると、拾う範囲と色を付ける範囲が食い違う
- 正式名（「創世記」など）には掛けない。ふつうの文の中で「今日はマタイの福音書 5章」と
  書かれたときに拾えなくなるため

### 「この箇所には過去のメモがあります」（`pastNotes` / `recordScopeRefs`）

入力画面の札。**閲覧画面の `relatedRecords` とは別物なので、混ぜないこと。**

| | 入力画面（`pastNotes`） | 閲覧画面（`relatedRecords`） |
|---|---|---|
| 細かさ | 節まで（`refsOverlap`） | 章まで |
| 手がかり | 箇所の欄だけ | 本文ぜんたい |
| 件数 | 6件 | 20件 |

- **手がかりはこちらの「箇所」の欄だけにすること。** メモ欄まで見ると、
  打っている途中で札が出たり消えたりして落ち着かない
- 通読は「選んだ書・章」を使う。**選んでいないときは手で書いた `passageText` から読み取ること**
  （書・章を選ばずに直接書く人がいる。以前はそのとき札が出なかった）
- 相手の記録が「どこを扱っているか」は `recordScopeRefs` が決める。
  **`recordRefs` を使わないこと。** あちらは探すの絞り込みや実績の集計に使うもので、
  結果を変えると別の画面に影響が出る

#### 章だけの言い及びは、同じ章に節つきがあるときは数えない（`narrowRefs`）

学びの記録で、聖書箇所が「ヨハネの福音書 3:1-12」でも、メモに
「ヨハネ3章の前半について」と書いてあると、章だけの箇所が拾われて
章ぜんたいを扱ったことになり、「3:13-24」を書いているときに並んでいた（実際そうなっていた）。

- 同じ書・同じ章に節つきの箇所がひとつでもあれば、その章の「章だけ」の箇所は落とす
- **通読で選んだ書・章はこの対象外。** 「その章を読んだ」というはっきりした事実なので、
  メモに節つきの引用があっても章ぜんたいのまま残すこと。
  狭めてしまうと、3章の通読が「3:13-24」で見つからなくなる
- 試験は `/tmp/rtcheck2/past.cjs`（15通りの組み合わせを表で確かめる）

### 同じ箇所かどうかは節まで見る（`refsOverlap`）

記録画面の「この箇所には過去のメモがあります」は、
以前は書と章が同じというだけで拾っていた。そのため
「ヨハネの福音書 3:16」を書いているときに「3:5」のメモまで並んでいた。

- `refsOverlap(a, b)` … 書・章（範囲）・節（範囲）の重なりを見る
- **どちらかに節が無いときは、その章ぜんたいを指しているものとして重なりと見なす**
  （3章の通読は 3:16 を含む）
- 閲覧画面の `relatedRecords`（「この箇所を含む記録があります」）は**章の単位のまま**。
  言葉のとおり「含む」記録を広く見せたいので、あえて揃えていない

### 記録の種類の名前・説明は設定から外した

依頼により、カスタマイズ画面の「記録の種類」の欄を取り除いた。
**保存されている値（`typeDesc`）は消さないこと。** そのまま持ち回り、
保存のときも書き戻し、バックアップにも入れ続ける。
捨てると、以前に名前を変えていた人の設定が黙って初期値に戻る。
イラストの見出し（`mascotGroupLabel`）もこの名前から組み立てているので、
`nameDraft` を渡す道すじは残してある。

### 直近で直した不具合
- **章をまたぐ箇所（「創世記 2章-5章」）が、札に「創世記 2」としか出なかった。**
  `REF_REGEX` に章の範囲を読む枝が無かったため。
  枝を足し、`formatRef` が「創世記 2-5」と見せるようにした。
  **`REF_REGEX` の分かれ道は上から順に試されるので、並べ替えないこと**（節より先に章の範囲を試す必要がある）。
  あわせて `recordRefs` で間の章も数え上げるようにしたので、
  「4章」で探したときにもこの記録が見つかる。
  逆向き（5章-2章）やその書に無い章数は、範囲として扱わない
- **未保存の印（三本線の右上のオレンジ丸）が、タブの見出しでは真ん中寄りに出ていた。**
  印をボタンの角からの距離（`top:12 / right:9`）で置いていたため、
  ボタンがちょうど56pxのときしか合わなかった。
  ボタンとアイコンの大きさの組み合わせが変わると合わなくなり、
  タブの見出し（ボタン56px・アイコン32px）では印がアイコンに重なっていた。
  `MenuIconWithBadge` を作り、**アイコンを包んだ span を基準に置く**ようにした。
  これでボタンが何pxでも必ず右上に出る。
  **付属物の位置は、外側の箱ではなく「くっつけたい相手」を基準に置くこと**
- **三本線メニューから別の画面へ移ると、画面が2枚重なっていた。**
  すでに開いている画面を閉じずに次を開いていたため。重なる画面はどれも同じ高さ（z-index 50）なので、
  JSXで最後に書いてある「収穫した実」が常に手前に出続け、何を選んでも同じ画面に見えていた。
  `goFromMenu` で、開く前に4つの画面をすべて閉じるようにした。
  **重なって出る画面を増やすときは、必ずここにも1行足すこと**
- `<textarea>` は何も指定しないと「2行ぶん」の高さから始まる。
  そのため1行だけの欄が、となりの入力欄より2〜3割背が高く見えていた。
  `TextArea` に `rows={1}` を入れて解決（実際の高さは中身に合わせて伸びるので、書き足せば普通に広がる）。
  1行の入力欄と高さをそろえたいときは `className="ft-h-field"` を付ける
- 探す画面の、検索前の案内文とイラストを削除した（`search-empty` は結果なしの側でまだ使っている）

### それ以前に完了した内容（UIの手ざわり）
- 押したときの動きを `ft-*` の自前CSSに集約。
  **これまで `active:scale-[…]` で書いていた分は、アーティファクト版では効いていなかった**
- 下タブ・＋ボタン・途中保存・ピン留め・カレンダー・折りたたみ・果樹などに演出を追加
- `prefs.motion` を追加し、カスタマイズ画面に「押したときの動きをつける」スイッチを新設
  （古い保存ファイルには `motion` が無いが、`loadPrefs` が既定値と混ぜるので自動で「あり」になる）
- 試験の道具を `/tmp/rtcheck2/` に用意した（毎回消える。無ければ作り直すこと）
  - `check.sh` … ビルド＋**95画面のSSR描画試験**
  - `motion.cjs` … Chromiumで、演出が実際に動くか／止める指定が効くかを確かめる
  - `film.cjs` … 演出を時間を止めて撮り、1枚に並べて目で確かめる

### それ以前に完了した内容
- 三本線を大きくし、バッジを右上に移動（アイコン32px・押せる範囲56px角）
- ひつじのアプリアイコンを作成し、manifest と index.html に組み込み（zip側のみ。Claude版はアイコン指定不可）
- ファイル保存に失敗したとき `SaveFallbackDialog` を出し、そこからクリップボードへコピー
- 今年／今月の聖句を `VerseCard` に作り直し、「続きから」と同じ形にそろえた（旧 `VerseRow` は廃止）
- `CalendarView` を `HomeScreen` から `ProgressScreen` へ移動（使用箇所は1か所のまま）
- 実績画面を `BOOK_GROUPS` で8つに折りたたみ、章タイルは `BookRecordsScreen` の上部へ移した
- 実績画面のイラストを2枚から1枚（末尾のみ）に減らした
- ホームの「まだ書き出していない記録が…」を上から最後のカードへ移した（イラストの上）
- 三本線のバッジを数字から小さな丸に変えた
- ホーム下部のイラストを果樹の育成機能に差し替え、`home-foot` を `MASCOT_SPOTS` から削除した
- 記録のテキストエリア全9か所に「書・章・節を選んで挿入」（`RefInserter`）を付けた。追記は `appendRef` に集約
- はじめからの絵の選び方を `MASCOT_SPOTS` の並び順から**分類ごと**に変更（`slotFor`）。
  カスタマイズ画面の見本と実際に出る絵が21か所中17か所ずれていたため
- 保存・カスタマイズ・収穫の3画面に「戻る」ボタンを追加
- 「記録しました」の黒い通知は**廃止した**。復活させないこと

---

### 仕様書（Footprints仕様書.html）
`makedoc.js` で作る。**段階の条件表とみことばは `BibleTracker.jsx` の `STAGES` から読み取っている。**
本体を直したら `node makedoc.js` を実行し直せば資料もそろう。手で書き写さないこと。

### 説明文の置き場所
使い方の説明は、次の2か所だけに置く。画面に出しっぱなしにしないこと。
- メニューの「ヘルプ」（`HELP_SECTIONS`）… まとまった使い方
- 「？」の吹き出し（`HelpTip`）… その項目だけの短い説明

### 言葉の決まり
- 聖書の各書は「**書**」と呼ぶ（「書簡」とは呼ばない）
- 並べ替えの「**目次順**」＝聖書の目次の並び順。
  内部の値は `"book"` のままなので、**画面に出る文字だけを変えること**

## 8. 好みの傾向（これまでのやりとりから）

- ボタンは**高さ40px**（`.btn-h`）。大きすぎるのを何度も嫌がられた
- 色は深いティール基調。**派手な色・オレンジ系のボタンは好まれない**
- 説明文は短く。長い注意書きは削られる
- アニメーションは、以前は不要と言われて撤去したが、**あらためて「楽しい動きがほしい」と依頼があり入れ直した**（`ft-*`）。
  ただし方針は変わっていない：**小さく・短く・押した場所で返す**。
  大きく動かす／画面全体を派手に飛ばす／通知やポップアップで祝う、はいずれも不可。
  気に入らなければカスタマイズ画面のスイッチ一つで全部止められるようにしてある
- 連続日数のような「途切れると落ち込む」仕掛けは**避ける**方針
- 入力の妨げになる装飾は嫌う。イラストは余白・末尾・空の画面にだけ置く
- **1画面にイラストは1枚まで。** 多いと指摘された
- 通知や催促は目立つ場所に置かない。ホームの案内は下のほう。ただしイラストより下には置かない（画面はイラストで締める）
- カードは「白地・角丸2xl・太さ2の枠・左に丸い四角のアイコン・小さなラベル＋本文」で統一されている。新しいカードもこの形に合わせること
