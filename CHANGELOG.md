# 変更のあゆみ

版数は `index.html` の `window.__FT_VERSION` 1か所で決めています。
上げたら、この表にも1行足してください。

## 数の上げかた（X.Y.Z）

| どこ | 呼び名 | どんなとき |
|---|---|---|
| **X**.0.0 | メジャー | 見た目や操作性に影響する**大きな変更**、ページの追加 |
| 1.**Y**.0 | マイナー | **細かな機能向上**、部分的な情報の追加、ページの修正 |
| 1.0.**Z** | パッチ | **不具合の直し**、誤字脱字の訂正 |

- 上を動かしたら、下は0に戻します（1.2.3 のあとにマイナーを上げたら 1.3.0）。
- **Z（パッチ）は「直し」だけ**です。動きや見た目を新しく変えたのなら、
  どんなに小さくても Y 以上を上げます。
- 一度に複数入れたときは、**いちばん大きいところ**を上げて1行にまとめます。

---

## 2.7.0

- **記録の写真の見え方と、大きく見る画面の動きを、姉妹アプリ My手帳（2.20.3）に合わせた**（見た目と操作が新しく変わるので、マイナーを上げた）
  - 閲覧画面の写真：2列・正方形の並びをやめ、1つの角丸の枠にすき間3pxで敷きつめる形にした。1枚は横長（4:3）で大きく、2枚は左右、3枚は左に縦長1枚＋右に2枚、4枚は2×2（枠は3:2）
  - 入力画面の写真の欄は、これまでどおり2列・正方形（✕で外したときの事故を防ぐため、枚数で並びを変えない）
  - 大きく見る画面：**二本指でつまんで拡大・縮小**（5倍まで）、**二度たたき（二度クリック）で拡大／もとの大きさ**、拡大中は指でずらして見られ、「もとの大きさ」のボタンが出る
  - **一度たたくと閉じる**（二度たたきを待つため、少しだけ遅れて閉じる）。拡大中の一度たたきでは閉じない
  - 下に払うと、下へ滑り出て閉じる。払ったぶんだけ地と✕・枚数がうすくなる。上へは少しだけ動く
  - 端の写真でさらに横へ払うと重くなる（先頭・最後に戻らない）
  - 左上に✕、右上に「2 / 4」、下に点を並べ、点を押すとその写真へ移る
  - 大きく見ている間の指の動きが、うしろの画面（左端から払って戻るなど）へ届かないようにした

## 2.6.2

- **「1行あけ」の空きが、引用のあとと、ふつうの文のあとで違っていた不具合を直した。** 聖書箇所の引用から1行あけて書くと、ふつうの文どうしの1行あけより狭く空いていた
  - 原因：ふつうの文の空行は、そのまま1行ぶんの高さで表示していた。引用のまわりの空行だけは、2.1.1 から0.75行ぶんに詰めていた
  - 直し方：ふつうの文の中の空行も段落の区切りとして扱い、**空行1つにつき本文ちょうど1行ぶん**を、どこでも同じ計算で空けるようにした
  - 空行を書かずに引用を続けたときの狭い間隔（6px）は、これまでどおり

## 2.6.1

- **「キャンセル」ボタンの枠が見えない不具合を直した。** 「保存されていない内容があります」と「同じ聖句が登録済みです」の小窓の「キャンセル」が、字だけで枠がなく、ボタンに見えなかった
  - 原因：目立たせないボタン（`BTN_QUIET`）だけ、枠も地の色も付けていなかった。他の画面の「キャンセル」「やめる」「閉じる」は、もともと枠つき（`BTN_SECONDARY`）だった
  - 直し方：`BTN_QUIET` に白地と細い灰色の枠を付けた。字の色は控えめなまま。この2か所以外に `BTN_QUIET` は使っていない

## 2.6.0

- **キーボードが出るとき、ヘッダーが一度下がってから戻る動きを直した。** 姉妹アプリ My手帳（2.18.2）のキーボードまわりの作りに合わせた
  - 原因：入力欄に直接フォーカスが入ると、iPhone は「入力欄がキーボードの裏になる」と見て画面ぜんたい（ヘッダーごと）を押し上げる。こちらの引き戻しと交互に描かれて、下がって戻る揺れになっていた
  - **入力欄に触れても、iPhone に直接フォーカスさせない**ようにした。見えない入力欄でキーボードだけを先に出し、押した欄を**実際に測ったキーボードの高さ**の上までゆっくり持ち上げてから、本物の入力欄へフォーカスを移す。ヘッダーは動かない
  - 持ち上げ方は場所ごとに違う：**下から出る紙**は箱ごと／**全画面（記録を書く画面など）**は送り場の中身だけ（見出しと下のボタンの帯は動かさない）／**下タブの画面（探す）**はページを送る
  - 押し上げようとして画面が送られる余地を作らないよう、重なる画面の外わくを `overflow: clip` にし、うしろを留める引き戻しからは `visualViewport` の resize を外した
  - 紙の中の字や余白を指で払っても、紙とうしろの一覧が動かないようにした（紙の中で本当に送れる一覧・テキスト欄は、これまでどおり送れる）
  - 覚えるキーボードの高さは端末ごと・向きごとに保存する（`bible-tracker-kb-last`）。はじめて出すときは画面の高さの50%を見込む
  - 「貼りつけて取り込む」と「タグの名前を変える」の小窓を、紙と同じ扱い（`ft-sheet-wrap`）にした。キーボードの上へ持ち上がる
- **キーボードを引っ込める操作を、My手帳に合わせた**（動きが新しく変わるので、マイナーを上げた）
  - 1行の入力欄で Enter を押すと、決定・作る・検索をすませてからキーボードが閉じる。テキスト欄（複数行）の Enter は改行のまま
  - **日本語入力の「変換を確定する Enter」では、検索やタグ作成が動かない・キーボードも閉じない**ようにした（以前は変換を確定しただけで動くことがあった）
  - 入力欄の外を軽くタップすると、キーボードが閉じる（ボタン・リンク・ほかの入力欄は除く。画面を送っただけでは閉じない）
  - 紙の暗がりを押すと、まずキーボードだけが閉じ、もう一度押すと紙が閉じる（打ちかけの文字を守るため）
- 長い文章の欄に触れたとき、カーソルは文字の最後に入り、画面はそこまで送られる（欄の途中を直したいときは、キーボードが出たあとにもう一度触れる）
- 文章の途中をタップしたとき、入力欄いっぱいに一瞬黒い膜が乗る不具合を直した（入力欄にもタップの膜を消す指定を足した）

## 2.5.0

- **書き出すファイルの名前から日付を外した。** 書き出すたびに毎回同じ名前になる
  - 対象は2か所：メニューの「バックアップ」と、1件の記録を「ファイルにして送る」
  - バックアップ：`Footprints-backup-2026-09-20.txt` のような名前 → `Footprints-backup.txt` に固定
  - 1件の記録：`Footprints-record-2026-09-20.txt` のような名前 → `Footprints-record.txt` に固定
  - 同じ場所に保存し直すと、前のバックアップに上書きされる（端末やブラウザの設定によっては
    `Footprints-backup (1).txt` のように自動で番号が振られることもある。これは端末側の挙動で、
    アプリ側では制御していない）
  - いつのバックアップかは、ファイル名ではなく中身の1行目「書き出し日時: …」で分かる。これは変えていない

## 2.4.0

- **記録を書く画面に「途中保存」のボタンを戻した**（2.0.0で外していたもの）
  - 置いた場所は、ヘッダーの右はし。それまでピン留め・ブックマークが並んでいたところ
  - 押すと、**画面は閉じずに**その時点の中身だけが残る。下の「保存」は、保存して画面を閉じる
  - 同じ記録に上書きしていくので、**何度押しても記録は増えない**
  - 押したあと1.8秒だけ、下向き矢印がチェックに変わってボタンが濃い緑に塗られる。波紋がひとつ広がる
  - 中身が空のあいだは、下の「保存」と同じように押せない
  - 途中保存したあと何も書き足していなければ、×で閉じても「保存されていません」と尋ねない
- **記録を書く画面から、ピン留めとブックマークのアイコンを外した**
  - この2つは記録を開いた画面（閲覧画面）から付け外しできるので、書く場所には置かないことにした
  - すでに付けてある印が消えるわけではない。付け外しする場所が1か所にまとまっただけ

## 2.3.3

- **起動の画面で、アイコンが上から下へ動いて見える不具合を直した**（ホーム画面に追加したアイコンから開いたときだけ起きていた）
  - 原因：iPhoneはホーム画面から開いた直後、本体の高さがいちど変わる。アイコン・名前・版数を、
    その変わる高さのまんなかに置いていたため、変わった分の半分だけ、かたまりごと下へ動いて見えていた
  - 直したのは、動く分の位置だけ。アイコンが最後に落ち着く場所や、起動の画面が消えるまでの動きは変えていない
  - ブラウザ（Safariなど）のタブで開いたときは、もともと動きが起きていなかったので変えていない

## 2.3.2

- **ボタンの「1回押しても効かない」「押したあとも色や枠が残る」を直した**（全画面のボタン共通）
  - 色が変わる演出（hover）は、マウスのある端末でだけ効くようにした。スマホ・タブレットでは、押したあとに色が残らない
  - 押したあと、ボタンに残っていたフォーカス（枠や色が付いたままになる原因）を外すようにした。キーボードで操作したときの枠は残す
  - ピン留め・ブックマーク・入り切りのつまみ・下のタブなどの受け方を、ふつうの「クリック」1つにそろえた。押したとたんに動く仕組みと、あとから来るクリックを捨てる仕組みが重なっていて、少しずれて押すと取りこぼすことがあった
  - 画面が切り替わるボタンで、切り替え先の処理がつまずいても、ボタンが沈んだまま押せなくなることがないようにした
  - ドラム式の選択（書・章・年月）で、行をタップして選ぶときに、押した瞬間に箱が指を捕まえないようにした（引っぱって動かしたときだけ捕まえる）

## 2.3.1

- バックアップ画面の説明文を大きく減らした。何が入っているかの説明文、「控えのとり方は2とおり」「記録が消えてしまうとき」の囲みは削除。書き出していないときの注意は見出し1行だけに

## 2.3.0

- **ヘッダーの背景：写真を選んだあと、帯にする場所を自分で決められるようにした**（姉妹アプリ My手帳 2.11.21 と同じ動き）
  - 選ぶと下から「帯にする場所を決める」が開く。窓は**いまのヘッダーの帯と同じ形**なので、決めた範囲がそのまま帯に出る
  - 指で動かす／2本指でつまんで大きさを変える（1〜4倍）。外へ引いても少しだけ出て、指を離すと戻る
  - カスタマイズ画面の見本も、帯と同じ形で出すようにした（高さの決め打ちをやめた）
  - これまでに設定した画像はそのまま使える

## 2.2.3

- 写真：**0枚のときは点線の大きな箱をやめ、「写真を追加（あと4枚）」のボタン1本だけ**にした（1枚以上のときと同じ見た目・同じ余白）
- 写真：**枚数にかかわらず2列・正方形**にした（1枚めだけ大きくならない）。閲覧画面も同じ
- 写真：上限に達したときの「写真は 4 枚までです」の知らせをやめた（ボタンに「あと○枚」「写真は 4 枚まで」と出るため）。読み込めない画像の知らせは残してある

## 2.2.2

- **起動の画面（アイコンと版数）で、下の帯が一瞬見えてしまう不具合を直した**。覆いが消え始めるまで、本体を見えなくしておくようにした（覆いの大きさも、下の端まで確実に届くようにした）

## 2.2.1

- **写真の✕で1枚外すと、もう1枚も消えてしまう不具合を直した**（写真が2枚のとき）。押したあと残りの写真が大きく描き直され、その✕が指の下に来て、遅れて届いた押し込みを受けていた

## 2.2.0

- **記録に写真を付けられるようにした**（どの種類でも、いちばん下・タグの上。1件につき4枚まで）。札には小さな見本が並び、閲覧画面では押すと大きく見られる（横に払って次の写真、下に払って閉じる）
- **写真の持ち方**：絵の中身は記録とは別の置き場に入れ、記録には参照だけを持たせる（写真で記録の保存が詰まらないように）。記録を消すと写真も片づく
- **ヘッダーの背景画像の持ち方も同じに直した**。これまで設定と同じ場所に絵の中身を丸ごと入れていたのをやめ、消えても戻せるように控えも持つ。これまでに設定した画像はそのまま使える
- バックアップに写真も含めるようにした（読み込むと元どおりになる）

## 2.1.1

- **聖書箇所（引用）と引用のあいだを詰めた**。空行を書いていないときはごく狭く、空行を書いたときも、そのままの高さは空けないようにした
- 重なって出る画面の取りこぼしを直した（キーボードぶんの余白が足されない画面があった）
- 聖句の「メモ」欄にも、読み取れた聖書箇所の札を出すようにした

## 2.1.0

- **聖書箇所の章・節の選び方を、以前の窓に戻した**。あわせて、**「聖書箇所を挿入」のボタンを入力欄の枠の中（下の帯）に入れ**、読み取れた聖書箇所の札も同じ帯に並べた（どの欄に入るのかが分かるように）
- **「〇〇を聖句に追加」を無くした**（数が多いと選びにくく、間違えて押しやすかったため）
- **聖書箇所＋1行あける＋聖書箇所の余白の不具合を直した**（書いた空行より広く空いていた）
- 探す：**当たった語を色で塗る**。見えていない場所に当たったときは抜き出しを出す
- 探す：**長押しで選ぶモード**を追加。まとめて削除できる
- 並べ替えを、端末まかせの選択画面から、押すたびに入れ替わる小さなボタンにした
- 削除や「保存せずに閉じる」のあとに、書きかけが戻ってこないようにした
- タグを選ぶ紙の説明文を外した

## 2.0.0

見た目と操作性が大きく変わったので、メジャーを上げた。姉妹アプリ My手帳（2.11.18）の作りに合わせている。
飾りの動き（`ft-*`）は、依頼により**そのまま残した**。

- 下タブ4画面の**見出しと検索の帯を、画面に固定**した（`sticky` をやめた。iPhoneで下へ送ると透けて消える不具合の予防）
- **×・キャンセル・戻るを、待たずに閉じる**ようにした（保存も押した瞬間に行う）
- **下のタブをもう一度押す**と、1回めは一番上へ、2回めは「探す」をはじめの状態へ戻す
- **値が変わるだけのボタン**（ピン留め・ブックマーク・タグ外し・絞り込みの札・スイッチ・下のタブ）を、指を離した瞬間に受ける方式にした
- 記録を書く画面：**日付を1行に畳んで先頭**へ、**タグを最下段**へ。ヘッダーを「× ／ 種類名 ／ 元に戻す・やり直す・ピン留め・ブックマーク」にし、**途中保存ボタンを外した**。保存は中身が空のあいだ押せない
- **聖書箇所を挿入**を、下から出る紙1枚に作り替えた（章・節の「はじめ／おわり」をドラム4本で選ぶ）
- 「今月／今年の聖句にする」を**スイッチの2行**に
- ドラムの紙の足もとを「キャンセル／決定」にそろえた
- 探す画面の絞り込みから、項目名と「？」を外した（「期間」だけ残す）
- 「何を記録しますか」の**説明文を外した**
- 重なる画面のうしろを、**位置を覚えて引き戻す**方式で留める（21か所の紙・小窓に適用）。キーボードに隠れる高さぶんの余白を先に足す。左端から払って戻るとき、暗がりも薄くする
- カードを押したときの沈み込みをやめ、薄い膜だけにした（写真つきの札の上から画面が送れなくなる不具合の予防）
- 本文のURLを「つかめない」ようにした（リンクの上から画面が送れなくなる不具合の予防）

## 1.1.0

- 起動の画面の表示時間を1秒から**2秒**に伸ばした
  （不具合の直しではなく見せかたの調整なので、パッチではなくマイナー）

## 1.0.0

版数を付け始めた最初の版。

- **起動の画面**を作った（双葉・アプリ名・版数。見せてからフェードアウト。くるくるは廃止）
- 「探す」の**検索欄と絞り込みの帯を、見出しの下に貼りつけた**
- 記録を書く画面から**項目名をすべて外した**（欄の中の薄い字で示す）
- 「書・章・節を選んで挿入」を**「聖書箇所を挿入」**に改め、欄のすぐ下へ寄せた
- 「この箇所を含む記録」が、**重なっていない箇所まで拾っていた不具合**を直した
- 各画面のイラストを約1.2倍に
- **枠の線を1pxに**、項目のあいだの余白を16pxにそろえた
- **タグの並べ替え**を、札の実際の位置を見て入れ替える方式にした
- 端末に「記録を消さないでほしい」と**頼むようにした**（`navigator.storage.persist`）
- メニューとバックアップ画面に、**最後に書き出した日**と未書き出しの件数を出した
- ヘッダーの背景を、**黒い膜＋白い文字**に改めた
- 字の太さを600までにそろえ、見出しの大きさを調整した
- 下の帯（タブ）と右下の丸ボタンの位置を作り直した
